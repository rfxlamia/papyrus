[package]
name = "papyrus-core"
version = "0.1.0"
edition = "2021"

[dependencies]
lopdf = "0.35"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pulldown-cmark = "0.10"
